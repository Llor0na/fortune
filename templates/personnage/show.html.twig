{% extends 'base.html.twig' %}

{% block title %}Personne{% endblock %}

{% block body %}
    <h1>Personne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personne.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personne.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ personne.prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ personne.telephone }}</td>
            </tr>
            <tr>
                <th>DateDeNaissance</th>
                <td>{{ personne.DateDeNaissance ? personne.DateDeNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ personne.job }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_personnage_index') }}">back to list</a>

    <a href="{{ path('app_personnage_edit', {'id': personne.id}) }}">editer</a>

    {{ include('personnage/_delete_form.html.twig') }}
{% endblock %}
